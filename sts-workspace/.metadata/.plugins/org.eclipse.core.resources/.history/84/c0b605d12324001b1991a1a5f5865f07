package com.lyncwork.services.store.service.mapper;

import com.lyncwork.services.store.domain.BasicRole;
import com.lyncwork.services.store.service.dto.BasicRoleDTO;
import java.util.ArrayList;
import java.util.List;
import javax.annotation.processing.Generated;
import org.springframework.stereotype.Component;

@Generated(
    value = "org.mapstruct.ap.MappingProcessor",
    date = "2020-11-10T19:26:39-0500",
    comments = "version: 1.3.1.Final, compiler: javac, environment: Java 11.0.9 (Oracle Corporation)"
)
@Component
public class BasicRoleMapperImpl implements BasicRoleMapper {

    @Override
    public List<BasicRole> toEntity(List<BasicRoleDTO> dtoList) {
        if ( dtoList == null ) {
            return null;
        }

        List<BasicRole> list = new ArrayList<BasicRole>( dtoList.size() );
        for ( BasicRoleDTO basicRoleDTO : dtoList ) {
            list.add( toEntity( basicRoleDTO ) );
        }

        return list;
    }

    @Override
    public List<BasicRoleDTO> toDto(List<BasicRole> entityList) {
        if ( entityList == null ) {
            return null;
        }

        List<BasicRoleDTO> list = new ArrayList<BasicRoleDTO>( entityList.size() );
        for ( BasicRole basicRole : entityList ) {
            list.add( toDto( basicRole ) );
        }

        return list;
    }

    @Override
    public BasicRole toEntity(BasicRoleDTO dto) {
        if ( dto == null ) {
            return null;
        }

        BasicRole basicRole = new BasicRole();

        basicRole.setId( dto.getId() );
        basicRole.setRoleName( dto.getRoleName() );

        return basicRole;
    }

    @Override
    public BasicRoleDTO toDto(BasicRole entity) {
        if ( entity == null ) {
            return null;
        }

        BasicRoleDTO basicRoleDTO = new BasicRoleDTO();

        basicRoleDTO.setId( entity.getId() );
        basicRoleDTO.setRoleName( entity.getRoleName() );

        return basicRoleDTO;
    }
}
