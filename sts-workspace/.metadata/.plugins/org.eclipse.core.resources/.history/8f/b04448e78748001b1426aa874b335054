package xyz.pakwo.expensify;

import java.time.Instant;
import java.util.Random;
import java.util.UUID;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.openfeign.EnableFeignClients;

import xyz.pakwo.expensify.service.PolicyService;

@SpringBootApplication
@EnableFeignClients
public class ExpensifyClientApplication implements CommandLineRunner {

	@Autowired
	private PolicyService policyServiceRestTemplate;
	
	@Autowired
	private PolicyService policyServiceOpenFeign; 
	
	public static void main(String[] args) {
		SpringApplication.run(ExpensifyClientApplication.class, args);
	}

	@Override
	public void run(String... args) throws Exception {
		
		/**  >>> Rest Template <<< */ 
		
		//policyServiceRestTemplate.getPolicies();
		
       policyServiceRestTemplate.createPolicy("Policy Rest Template -" + Instant.now());
																	
     // policyServiceRestTemplate.updatePolicy("18D8F90060145508", "employee.csv");
		
		
		/**  >>> OpenFeign handler <<< */ 
		
	//	policyServiceOpenFeign.getPolicies();
		
//		policyServiceOpenFeign.createPolicy("Policy-OpenFeign-" + Instant.now());
		
		// TODO : fix the upload issue
//		policyServiceOpenFeign.updatePolicy("7FCC1FEB44457404", "employee.csv");
	}

}
