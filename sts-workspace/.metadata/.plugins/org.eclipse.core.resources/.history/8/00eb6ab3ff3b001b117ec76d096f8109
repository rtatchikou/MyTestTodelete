#! /bin/bash

environment="$1"
GUSTO_TAG="387856044462.dkr.ecr.us-east-1.amazonaws.com/gusto"
 if [ "${environment}" = "prod" ]; then
        environment = "latest"
fi

    docker tag gusto ${GUSTO_TAG}:${environment} && \
    docker push ${GUSTO_TAG}:${environment}

exit 0;
