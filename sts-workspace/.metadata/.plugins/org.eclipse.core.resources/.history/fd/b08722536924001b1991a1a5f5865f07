package com.lyncwork.services.store.service.mapper;

import com.lyncwork.services.store.domain.BasicTeam;
import com.lyncwork.services.store.service.dto.BasicTeamDTO;
import java.util.ArrayList;
import java.util.List;
import javax.annotation.processing.Generated;
import org.springframework.stereotype.Component;

@Generated(
    value = "org.mapstruct.ap.MappingProcessor",
    date = "2020-11-09T21:52:48-0500",
    comments = "version: 1.3.1.Final, compiler: javac, environment: Java 11.0.9 (Oracle Corporation)"
)
@Component
public class BasicTeamMapperImpl implements BasicTeamMapper {

    @Override
    public List<BasicTeam> toEntity(List<BasicTeamDTO> dtoList) {
        if ( dtoList == null ) {
            return null;
        }

        List<BasicTeam> list = new ArrayList<BasicTeam>( dtoList.size() );
        for ( BasicTeamDTO basicTeamDTO : dtoList ) {
            list.add( toEntity( basicTeamDTO ) );
        }

        return list;
    }

    @Override
    public List<BasicTeamDTO> toDto(List<BasicTeam> entityList) {
        if ( entityList == null ) {
            return null;
        }

        List<BasicTeamDTO> list = new ArrayList<BasicTeamDTO>( entityList.size() );
        for ( BasicTeam basicTeam : entityList ) {
            list.add( toDto( basicTeam ) );
        }

        return list;
    }

    @Override
    public BasicTeamDTO toDto(BasicTeam entity) {
        if ( entity == null ) {
            return null;
        }

        BasicTeamDTO basicTeamDTO = new BasicTeamDTO();

        basicTeamDTO.setId( entity.getId() );
        basicTeamDTO.setTeamName( entity.getTeamName() );
        basicTeamDTO.setTeamColor( entity.getTeamColor() );

        return basicTeamDTO;
    }

    @Override
    public BasicTeam toEntity(BasicTeamDTO dto) {
        if ( dto == null ) {
            return null;
        }

        BasicTeam basicTeam = new BasicTeam();

        basicTeam.setId( dto.getId() );
        basicTeam.setTeamName( dto.getTeamName() );
        basicTeam.setTeamColor( dto.getTeamColor() );

        return basicTeam;
    }
}
